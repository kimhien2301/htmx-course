<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">
    <script src="https://unpkg.com/htmx.org@2.0.3"></script>
    <title>Todo</title>

    <script>
        htmx.on("htmx:responseError", (evt) => {
            console.log(evt)
            alert(evt.detail.xhr.response)
        })
    </script>
</head>
<body>

<div class="container mt-2">
    <div class="row">
        <div class="col">
            <h2>Task List</h2>
            <div>
                <button hx-get="/tasks" hx-trigger="click" hx-target="#taskList"
                        class="btn btn-primary">
                    Load Task
                </button>
                <button type="button" class="btn btn-success"
                        hx-get="/get-task-form" hx-trigger="click" hx-target="#taskForm">
                    Add New
                </button>
            </div>
            <div id="taskList" hx-get="/tasks" hx-trigger="load"></div>
        </div>
        <div class="col">
            <h2>Task Form</h2>
            <div id="taskForm">
                <%- include('addTaskForm.ejs') -%>
            </div>
        </div>
    </div>
</div>


</body>
</html>